# Build products
build/
.build/
*.icns

# Design sync state. Holds a private Claude Design project id, so it stays local.
.design-sync/

.DS_Store

# Second-architecture scratch dir for the universal release build
.build-x86/
